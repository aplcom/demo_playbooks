- hosts: all
  gather_facts: no
#  become: true
  become_method: sudo
  become_user: root

  tasks:
  - debug:
      msg: "TEST_VARIABLE: {{ TEST_VAR_2 }}, TEST_VAR_2: {{ TEST_VAR_2 }}"  
    register: shell_output
  
  - name: copy the output to a local file
    copy:
      content: "{{ shell_output }}"
      dest: "/tmp/ansi_variables.txt"
    delegate_to: localhost  
  

